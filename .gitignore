# compiled binary (root only — must NOT match the cmd/t3scan/ source dir)
/t3scan

# logs / scratch
*.log

# local recon / scan outputs (never commit target lists)
/targets.txt
*.targets
*.scan

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
